# ------------------------------------------------------------------
# Project Info
# ------------------------------------------------------------------
SANCTION_PROJECT_NAME="AML Project"
SANCTION_PROVIDER_NAME="AmaniAI"

# ------------------------------------------------------------------
# App Configuration
# ------------------------------------------------------------------
# Max parallel scrapers (Integer)
SANCTION_SANCTIONS_CONCURRENCY=3

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
SANCTION_LOG_LEVEL="INFO"
CONTRACT_VERSION:str="1.0"
SCEMA_VERSION:str="1.0"
# ------------------------------------------------------------------
# Data Lake Configuration
# ------------------------------------------------------------------
# Path to the data directory. Can be relative or absolute.
# Default is ./data inside the current working directory.
SANCTION_DATA_LAKE_DIR="./data"

# Note: RAW_DIR and PROCESSED_DIR are computed properties based on
# DATA_LAKE_DIR and cannot be set explicitly here.

# ------------------------------------------------------------------
# Relationship Caps (for Entity Resolution)
# ------------------------------------------------------------------
SANCTION_MAX_RELS_PER_ENTITY=300
SANCTION_MAX_REL_ENDPOINTS_PER_SIDE=50